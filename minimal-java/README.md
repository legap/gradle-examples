# Minimal Gradle example with Java

Demonstrates a minimalistic example for a build definition in a Java project.

Run with:
```
java -jar build/libs/minimal-java.jar "Hello World" "Beautiful World!"
```
